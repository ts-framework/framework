{
	"private": true,
	"scripts": {
		"dev": "pnpm -r --parallel run dev",
		"test": "pnpm -r test --workspace-concurrency 1"
	},
	"devDependencies": {
		"@changesets/cli": "^2.22.0"
	},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [],
			"allowedVersions": {}
		}
	}
}
