{
	"private": true,
	"scripts": {
		"dev": "pnpm -r --workspace-concurrency=1 run build && pnpm -r --parallel run dev",
		"test": "pnpm -r --workspace-concurrency=1 test"
	},
	"devDependencies": {
		"@changesets/cli": "^2.23.2"
	},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [],
			"allowedVersions": {}
		}
	}
}
